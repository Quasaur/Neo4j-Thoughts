# Q:
Help me locate the Docker image python-3.13-slim (include the Docker Hub URL(s).
# A:
The Docker image for python-3.13-slim is available on Docker Hub.
You can find it at the following URL:
 * Docker Hub: https://hub.docker.com/_/python
The python-3.13-slim tag is part of the official Python image repository. When you visit the page, you'll see a list of available tags, including the various slim versions for different Python releases.
